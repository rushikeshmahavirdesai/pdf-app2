[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[dev]
  port = 8888
  publish = "public"
  functions = "netlify/functions"
  framework = "#static"
  autoLaunch = false
