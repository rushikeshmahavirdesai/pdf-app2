{
  "name": "katina-print-app",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Katina article print CSS and PDF tool for Netlify",
  "scripts": {
    "dev": "node server.js",
    "dev:netlify": "netlify dev --port 8888",
    "setup": "npx playwright install chromium"
  },
  "dependencies": {
    "@sparticuz/chromium": "^131.0.1",
    "playwright-core": "^1.49.1"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
